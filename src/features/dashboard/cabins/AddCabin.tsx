import Button from "@/components/ui/Button";
import { type ReactNode } from "react";


export default function AddCabin({}: Props): ReactNode {
  return <Button size="md">اضافه کردن سوییت</Button>;
}
