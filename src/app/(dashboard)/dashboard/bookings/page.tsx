import type { Metadata } from "next";

export const metadata: Metadata = { title: "مدیریت رزروها" };

function page() {
    return (
        <div>
            
        </div>
    )
}

export default page
